apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: k33-grafana-route
  namespace: monitoring
spec:
  routes:
    - match: Host(`grafana.purplepip.com`)
      kind: Rule
      services:
        - name: grafana
          port: 80
