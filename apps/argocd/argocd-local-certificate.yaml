apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-local-certificate
spec:
  secretName: argocd-server-tls
  commonName: argocd.local
  subject:
    organizations:
      - Adaptive Kind
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - argocd.local
